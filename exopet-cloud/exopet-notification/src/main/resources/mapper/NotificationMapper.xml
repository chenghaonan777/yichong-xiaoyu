<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exopet.notification.mapper.NotificationMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="com.exopet.notification.entity.Notification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="related_id" property="relatedId"/>
        <result column="is_read" property="isRead"/>
        <result column="read_at" property="readAt"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 通用字段列表 -->
    <sql id="BaseColumns">
        id, user_id, type, title, content, related_id,
          is_read, read_at, created_at
    </sql>


</mapper>