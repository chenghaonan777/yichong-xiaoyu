<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.exopet.hospital.mapper.HospitalReviewMapper">

    <resultMap id="BaseResultMap" type="com.exopet.hospital.entity.HospitalReview">
        <id     column="id"           property="id"/>
        <result column="hospital_id"  property="hospitalId"/>
        <result column="user_id"      property="userId"/>
        <result column="appoint_id"   property="appointId"/>
        <result column="rating"       property="rating"/>
        <result column="content"      property="content"/>
        <result column="created_at"   property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, hospital_id, user_id, appoint_id, rating, content, created_at
    </sql>

    <!-- ==================== 按医院ID查询评价（分页） ==================== -->
    <select id="selectByHospitalId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM hospital_review
        WHERE hospital_id = #{hospitalId}
        ORDER BY created_at DESC
    </select>

</mapper>
